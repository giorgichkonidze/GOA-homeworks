// დღეს მუშაობის დროს, იყო გასაკეთებელი პროეკტი,

// ეს ეგრედ წოდებული პროეკტი მოიცავდა: 

// ფუნქციებს სადაც იყო props-ები, შვილობილი კომპონენტები, perent

// ჩვენ გამოვიყენეთ function სადაც დარენდერება მოხდა არაი ფუნქციით.

// ასევე გამოვიყენეტთ export and import ები.

// რითიც ვმართეთ კოდი, და კომპონენტები.

